#class wudang open;

#var bedroom 12;
#var kitchen 28;
#var lwroom 17;
#var food tao;
#var drink wan;

#alias runfzg {#map run 复真观二层};
#alias runjxg {#map run $lwroom};

#alias getfood {
    #map get roomname {kitchenname} {$kitchen};
    #act {^$kitchenname -} {sit chair; knock table; get $food; get $drink; eat $food; drop $food; drink $drink; drop $drink; #delay 2 {%1}; #unact {^$$kitchenname -}};
    #map run $kitchen; 
};

#alias wdquest {
    #var waiting.action {%1 %2};
    #var do-xiake %3;
    #read bot/wudang/quest.tt;
    AQ;
};

#alias wdquestx {
    #class waiting kill;
    #class wdquest kill;
};

/*
#act {^你在攻击中不断积蓄攻势。(气势：%d} {
    #if {%1 >= 8} {perform sword.chan};
};
*/

#act {^你眼前一花，但见四面八方都是%*的人影} {
    #class pfm open;
    #act {^你在攻击中不断积蓄攻势。(气势：%d} {
        #if {%1 >= 8} {perform sword.chan};
    };
    #class pfm close;
};

#alias lifepfm {pchan};

#act {( 只听见「啪」地一声，你手中的%*已经断为两截！ )} { wield sword};

#alias wields {unwield all; wield sword; #delay 1 wield blade}
#alias pchan {wield sword; perform sword.chan};
#alias plian {wield sword; perform sword.lian};
#alias pzhen {unwield all; perform cuff.zhen; wield sword; #delay 1 wield blade;};
#class wudang close;

