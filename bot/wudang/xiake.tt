#class xiake open;

#alias xiake.start {
    #action {^草屋 -} {
        ask cui about 麻烦;
        #unaction {^草屋 -};
    };
    #map run {草屋} {0.2};
};

#action {崔老汉说到：前几天村子里面来了几个土匪，把过世的老伴留给我的银镯子给抢走了，一定要帮我拿回来！} {
    run 954;
};
